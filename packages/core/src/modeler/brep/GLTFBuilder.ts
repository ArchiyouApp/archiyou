// Re-exported from unified src/GLTFBuilder — supports both brep and mesh pipelines.
// NOTE: ExplodedViewOptions/LayoutViewOptions used to be re-exported here too and no longer
// exist in that module — they live in ../types. Import them from there.
export { GLTFBuilder } from '../../GLTFBuilder'
