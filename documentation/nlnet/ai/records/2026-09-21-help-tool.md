# Help tool: onboarding, tutorials, API reference

| | |
|---|---|
| Dates | 2026-09-21 → (open) |
| Model | Claude Opus 5 (claude-opus-5[1m]), 1M context, Claude Code agent; research and plan by Claude Fable 5.1 (claude-fable-5-1) |
| Tool | Claude Code in plan mode (with Explore subagents for code and web research), then as agent |
| Human | Mark van der Net: wrote the brief and prompts, chose the options in the plan, reviewed plan and code |
| Branch | `help` |
| Session transcript | kept locally; the prompts are reproduced in full below |

## Prompts (verbatim, local time)

(filled when the unit closes)

## Review and decisions by the human

- Chose: no docs generator yet; one markdown file per tutorial with `## ` steps; content bundled with the editor; 12 target languages including CJK and Arabic.
- Asked to add the old archiyou-core docs as a reference for later content, without porting now.
- Asked to do the first phases in a separate worktree on branch `help`.
- The `recipe` branch had been merged into `develop` in the meantime, so `help` branches from `develop`.
- After trying stages 0–3: asked for a thumbnail per tutorial (an image next to the markdown, named in the frontmatter), a `tags` field with tabs beginner | advanced | practical | modeling | documentation | IO | publishing, images that fill the panel width (animated ones for the tour), and a help icon in the left menu above settings. Reported the right toolbar help icon as missing.
- Reviewed the result in the editor and steered the layout step by step: thumbnails beside the tutorial text instead of above it, square, then half the size; card text small / extra small; no step count; smaller tags; portrait images in a 16:9 box, centred.
- Then moved the help button to the top of the right toolbar and removed the left-menu help button again.
- Removed the dimming rule on the Help item of the hamburger menu himself, and asked to rename `main-menu-file-menu.ts` to `main-menu-hamburger.ts` (the element and class were renamed with it).
- Asked to continue with the next phase (stage 4, API reference). Deviations the agent made from the plan, reported to the human: the slim reference data is committed (`api.generated.json`, like `completions-data.generated.ts`) instead of generated at build time; reference examples get a Copy button, not Apply + Run, so they never overwrite the user's script; the older fragment examples in `packages/meshup` (a submodule) are skipped by the example test until the human decides on editing meshup.
- Decided that the meshup doc comments are his to improve later; the skip list shrinks as the fragments are rewritten there.
- 2026-09-22: asked to set up the docs site (Astro Starlight) now, serving from the same help/ folder with the layout `help/<section>/<locale>/`, following Starlight's documentation for localisation, and to copy the old archiyou-core docs into it. The agent converted the VuePress guide with a one-off script (not in the repo): titles into frontmatter, asides, images next to their pages, links to site routes, and a banner saying the pages were written for an earlier Archiyou.
- Then tuned the site's look (editor font, header logo and colour, smaller titles), asked for a 400 px copy of the header logo, and asked to include the API reference on the site. The agent generated the reference pages from the same api.generated.json the editor uses, and moved FACTORY_RETURN_TYPES to core so the data itself says `box()` returns a Mesh.
- Directed the reference entry page: smaller title, no signature block, extra-small description, "Arguments" as one line each (bold name, italic type, "(optional: default …)" last), indented sections, search field as tall as the follow-cursor button, follow-cursor on by default.

## Commits

| Commit | Subject | Prompt it answers |
|---|---|---|

## Plan (agent output, reviewed by the human before implementation)

## Archiyou Help — research, design and staged plan

### Context

Archiyou has no onboarding, tutorials or in-app reference. New users land in a code editor with a
dimmed, dead `?` button (`packages/ui/src/editor/main-menu.ts:75`, "Not built yet"). We want one
**help tool** in the right toolbar with modes *Onboarding*, *Tutorials*, *API reference* (AI chat
later), driven by **markdown in the open-source repo** so content is easy to iterate on,
contribute to, translate with AI, and later publish on docs.archiyou.com without rewriting.

#### Decisions taken (with the user, 2026-09-21)

| Question | Decision |
| --- | --- |
| Standalone docs site | **Deferred.** Build the in-editor tool now; keep the format generator-neutral (Starlight recommended later, see "Research") |
| Tutorial layout | **One `.md` per tutorial, `## ` = step**, code-fence meta words drive the editor |
| Delivery | **Bundled with the editor** (Vite lazy raw imports), no runtime fetch |
| Languages, first wave | en source + nl, de, fr, es, pt, it, pl, tr, **ja, zh-CN, ko, ar** (CJK + RTL from day one) |

#### What exploration found (shapes everything below)

- **Editor is Lit 3 + Web Awesome + CodeMirror 6 + `@lit-labs/signals`** — no Vue. Components live in
  `packages/ui`, state in `apps/editor/src/state/*`.
- **A generic tool abstraction exists**: `ToolDef` (`packages/ui/src/editor/toolbar.ts:9`), registered in
  one literal array `TOOLS` (`apps/editor/src/pages/editor.ts:53`). Tools get no props; they read signals.
- **Markdown is solved**: `renderMarkdown()` in `packages/ui/src/utils/markdown.ts` (markdown-it +
  DOMPurify, `html:false`). House style for a markdown browser: `packages/ui/src/editor/modules-menu.ts`.
- **Context-lookup machinery exists**: `buildScopeTypeMap`, `extractChainRoot`, `resolveType`,
  `FACTORY_RETURN_TYPES` in `packages/ui/src/editor/completions.ts:68-165`.
- **Hard parts**: (a) every tour target sits 4-6 shadow roots deep and almost nothing has a stable hook
  (`id="tool-btn-${i}"` is positional); (b) TSDoc is thin (`Modeler.ts`: 0 `@param`, 2 `@example`;
  `Fab.ts`: none) and the `$` globals are defined inline in `Runner.ts:484-546`, so typedoc has no symbol
  for `$component`; (c) `@lit/localize` is half-wired (only `nl`, generated locale modules missing) and
  the editor has no locale switcher.

### Research summary

**Docs generators** (for later). VitePress 1.6.4: lightest, but no i18n fallback (missing page = 404),
MiniSearch indexes nothing for CJK, 2.0 alpha for a year, and its Vue advantage is worthless here.
**Astro Starlight 0.42: recommended when the site is built** — untranslated pages fall back to English
with a notice, RTL, Pagefind per-language index incl. CJK, web components render natively,
`starlight-typedoc` for the reference. Rejected: Docusaurus (React, heavy), Fumadocs (Next), Nuxt
Content/Docus (whole Nuxt runtime), MkDocs Material (maintenance mode since Nov 2025), Mintlify (paid).

**Exemplars and what we copy**
| Product | Copy |
| --- | --- |
| Strudel REPL | one JSON generated from source comments feeds docs *and* in-app reference; **every example is executed in CI** |
| Godot editor | F1 = search help, Shift+F1 = help for symbol at cursor; reference is shipped data, not a link |
| Vue / Svelte tutorials | plain markdown steps + code injected into a live editor; "reset/show me" affordances |
| Processing "Find in Reference" | dumb token lookup is enough; ambiguity → show all owners |
| model-viewer docs | poster → click-to-activate for heavy viewers (docs site, later) |

**Tutorial encoding.** Conventions that survive GitHub, any static generator and an LLM translator:
flat frontmatter, **code-fence meta words**, **HTML comments**. Avoid MDX/Markdoc/`:::` directives
(render as junk on GitHub, translators mangle them, the in-app panel would need extra parsers).
No established prior art for DOM targets in markdown — we define our own comment directive.

**Tour libraries.** Shepherd (since v14) and Intro.js are AGPL — out. driver.js 1.8 (MIT, ~5 kB gz) is
the good one, but our tour text lives *in the help panel*, so we only need a spotlight, not popovers:
**~80 LOC of our own** (fixed div + `box-shadow: 0 0 0 9999px`, tracked with ResizeObserver) and no
dependency. driver.js stays the fallback if that proves fiddly.

**Context lookup.** Token + receiver heuristic (what Godot/Processing do) covers fluent CAD scripts;
a TypeScript language service in the browser (~1.5 MB gz) is not justified.

**AI translation.** Lingo.dev / "Markdown Translator" action validate the design: per-file source
hash stored *in the translated file*, mask code before sending, whitelist frontmatter keys, glossary,
structural validation, never overwrite human edits. ~180 LOC script, no SaaS.

#### Existing content to start from

The old archiyou-core docs — https://github.com/ArchiyouApp/archiyou-core/tree/main/docs/src — hold
a lot of material that can seed the new tutorials, onboarding and reference prose. Reference only
for now; **Mark will guide how it is used later**, so no porting happens in this round.

### Content format (the contract)

```
help/                          ← repo root; not "docs" (collides with the docs script module)
  glossary.json                ← do-not-translate list + per-locale term map
  en/
    onboarding.md              ← same format as a tutorial
    tutorials/simple-table.md, cabinet.md, csg.md, sketching.md, assets.md,
              components.md, docs.md, calc.md
  nl/ de/ … ar/                ← machine-translated mirrors (same paths)
```

```markdown
---
title: A simple table
description: Boxes, moves and parameters
level: beginner
order: 1
---

## Let's start with a box            ← every "## " starts a step
The table top is just a box.

```js run                            ← run    = replace the script with this, execute
top = box(1200, 700, 30)
```

## Add a leg
<!-- highlight: viewer -->           ← spotlight a registered UI target during this step
```js append                         ← append = add to the tutorial's code so far, execute
leg = box(60, 60, 720)
```                                  ← a fence without meta is display-only
```

- Frontmatter is **flat `key: value` only** → parsed in ~10 LOC, no YAML dependency.
- The player computes the code for step N by folding fences 1..N, so Back/jump always works.
- The tutorial chooser list is generated from the frontmatter of `tutorials/*.md` (sorted by `level`, `order`).
- `highlight` ids come from a frozen `HELP_TARGETS` list; a test fails on unknown ids.
- Docs-site later: `run`/`append` fences become poster + "Open in editor" (`/editor?tutorial=slug`);
  comments are invisible; nothing in the file is editor-specific syntax. `help/<locale>/` maps 1:1
  onto Starlight locale directories.

### The help tool

```
┌ Help ───────────────────────── [EN ▾] [×] ┐     Right toolbar: last button, pushed to the
│  Start │ Tutorials │ Reference │ (AI)      │     bottom (ToolDef gets optional align:'end').
├────────────────────────────────────────────┤     exclusive:false, width 32 → usable next to
│ TUTORIAL  A simple table        step 2 / 7 │     the console.
│ ## Add a leg                               │
│ prose…                                     │     Start      = onboarding.md in the same player,
│ ┌ js ───────────────────────┐              │                  with spotlight on highlight targets
│ │ leg = box(60, 60, 720)    │ [Apply+Run]  │     Tutorials  = chooser cards → player
│ └───────────────────────────┘              │     Reference  = search box + entry view +
│ [Back]  ● ● ○ ○ ○ ○ ○   [Next]   Restart   │                  "follow cursor" toggle
└────────────────────────────────────────────┘
```

**Onboarding**: first editor visit (no `localStorage['archiyou:help:onboarded']`) opens the tool in
Start mode. Steps highlight main menu, file menu, code box, Run (Ctrl+Enter), params, viewer, toolbar,
and end by offering the first tutorial. Skip/finish sets the flag; re-runnable from the Start tab.

**Tutorials**: starting one calls `createNewScript()` (user work is never overwritten). Next applies
the step code via `updateScriptCode()` and executes. Deep link `/editor?tutorial=<slug>` copies the
`_consumeNewQueryParam()` pattern (`editor.ts:368`).

**Reference — the hierarchy**
- Level 0 *Globals*: factory functions (`box`, `sketch`…), `$` functions (`$component`, `$pipeline`,
  `$module`, `$handle`, `$import`, `$PARAMS`), module objects (`docs`, `calc`, `make`, `fab`, `materials`…).
- Level 1 *Classes* (`Mesh`, `Curve`, `ShapeCollection`, `Document`, …) → members. Entry id = `Owner.member`.
- Entry: `{ id, name, owner?, kind, sig, returns?, summary, params[], examples[] }`; examples get the same
  Apply+Run button as tutorials.
- **Lookup at cursor** (F1, or live when "follow cursor" is on): word at cursor →
  1. not preceded by `.` → prefix match in Globals (`$comp` → `$component(`); a user `$WIDTH` → card
     explaining script params, linking `$PARAMS.define`;
  2. preceded by `.` → resolve receiver with the existing `extractChainRoot`/`resolveType`/scope map,
     then walk the chain through each entry's `returns` (`$component('x').params(` → `ComponentImporter.params`);
  3. receiver unknown → list every owner that has that member, common classes first.
- Search: scored prefix/substring over ~1200 entries in ~25 LOC — no search library.

### Execution now (requested 2026-09-21)

- Work in a **git worktree on a new branch `help`**, branched from `recipe` (the AI-disclosure tooling
  lives on `recipe`). Write this plan file (with the old-docs reference) to `plans/HELP.md` in the
  worktree and commit it first; remove the untracked copy from the main checkout so it lives only on `help`.
- Scope of this round: **Stages 0–3** (record, format + parser + first content, help tool + tutorial
  player, onboarding spotlight). Stage 4 (API reference) and later come after review.
- Install deps in the worktree (`pnpm install`, meshup submodule init) before tests; commits via
  `pnpm commit:ai`, no push.

### Stages

**Stage 0 — record.** Save this plan as `plans/HELP.md` (house format); open the AI-disclosure record
(`ai-disclosure` skill); commits via `pnpm commit:ai` on `recipe`.

**Stage 1 — format, parser, first content** *(~1.5 d)*
- `packages/ui/src/editor/help/help-content.ts` (~150 LOC, pure, no DOM): `parseHelpDoc(md)` →
  `{ meta, steps: [{ title, body, highlight?, fences: [{ lang, action, code }] }] }`, `codeAtStep(doc, n)`,
  `HELP_TARGETS`.
- Write `help/en/onboarding.md`, `tutorials/simple-table.md`, `tutorials/csg.md`
  (seed from `packages/core/tests/cadscripts/scripts/` — `maritavolo.js`, `slidercabinet.js`).
- Tests in `packages/ui/tests/help-content.test.ts`: parse, fold, unknown highlight id fails.
- Test beside `packages/core/tests/cadscripts/run.cadscripts.test.ts`: **execute every tutorial step
  headless** — broken tutorial code fails CI.

**Stage 2 — help tool + tutorial player** *(~3 d)*
- `apps/editor/src/state/help.ts` (~120 LOC): signals `helpMode`, `helpDoc`, `helpStep`, `helpLocale`;
  loader `import.meta.glob('/../../help/**/*.md', { query: '?raw' })` (lazy per file) with **fallback to `en`**;
  `applyAndRun(code)` = `updateScriptCode(code)` + `scheduleExecution()` (`state/viewer.ts:36`, existing indirection).
- `packages/ui/src/editor/tools/help-tool.ts` (~450 LOC, AGENTS.md 5-section order): tabs, chooser,
  player; render with `renderMarkdown()`; fences rendered by the component (not via HTML) so Apply+Run
  needs no raw HTML in markdown.
- `editor.ts`: add `ToolDef` + import, `_consumeTutorialQueryParam()`, first-run open.
  `toolbar.ts`: `align?: 'end'`, and `id="tool-btn-${tool.id}"` instead of the positional index.
- Remove the dimmed `btn-help` from `main-menu.ts`; wire the File-menu `help` item
  (`main-menu-file-menu.ts:132`, currently a TODO) to open the tool.

**Stage 3 — onboarding spotlight** *(~1 d)*
- Add `data-help="…"` to ~13 targets (`main-menu`, `file-menu`, `file-info`, `code`, `run`, `params`,
  `presets`, `viewer`, `viewer-menu`, `toolbar`, `tool-console`, `tool-docs`, `tool-help`).
- In `help-tool.ts`: `deepQuery('[data-help=x]')` (recursive through open shadow roots, ~15 LOC) and the
  spotlight (~80 LOC, a div on `document.body`, ResizeObserver + resize/scroll, Esc clears, `pointer-events:none`).
- Storage flag follows the `verify-email-banner.ts` try/catch pattern.

**Stage 4 — API reference** *(~3 d + enrichment)*
- Give the script scope a documented type: a `ScriptGlobals` interface with TSDoc **inside `Runner.ts`**
  next to `_addMetaMethodsToScopeState` (no new file) covering the `$` functions, `print/log`, module objects.
- `packages/core/scripts/generate-api.ts` (~150 LOC, beside `generate-completions.ts`): `typedoc --json`
  over core + meshup → flatten, render types to strings, fold the `declare module '@archiyou/meshup'`
  augmentations (`shapeAnnotations.ts`) into their classes, promote `MODELER_METHODS_INTO_GLOBAL`
  (`constants.ts:17`) to globals → `api.json` (gitignored, built in `turbo build` and `predev`).
  Same typedoc run later feeds the docs site via `typedoc-plugin-markdown`.
- `packages/ui/src/editor/help/help-reference.ts` (~200 LOC): lazy-load `api.json`, search, `lookupAt(doc, pos)`;
  export the four resolver helpers from `completions.ts` instead of copying them.
- `codebox.ts`: extend the existing `updateListener` (line 343) with `selectionSet` → debounced
  `cursor-change` event; add `F1` to the keymap. `editor.ts` forwards to `state/help.ts`.
- TSDoc enrichment: `@param` + one `@example` for the ~100 most used members (Modeler factories, Mesh/
  ShapeCollection transforms & booleans, Docs, Calc, `$` functions). CI executes `@example`s with the Stage 1 test.
- *Later, optional*: derive `completions-data.generated.ts` from `api.json` and delete the 487-LOC ts-morph generator.

**Stage 5 — localization** *(~2.5 d)*
- Finish `@lit/localize` wiring: `targetLocales` = the 12 locales, run `localize:build` so
  `locale-codes.ts` + locale modules exist; persist choice in `localStorage['archiyou:locale']`
  (distinct from `ay.locale`, which is configurator *content* locale).
- Locale switcher in `packages/ui/src/nav-bar.ts` (app-wide); the help header select is the same signal.
  Help panel sets `dir="rtl"` on prose for `ar`; code blocks stay `ltr`.
- `scripts/translate-help.mjs` (~180 LOC, `@anthropic-ai/sdk` devDependency, `pnpm help:translate [--locale] [--force]`):
  per file → sha of source → skip if target frontmatter `source-hash` matches or `translated: human`;
  mask fences, inline code, HTML comments as sentinels; translate only `title`/`description` + prose;
  inject `help/glossary.json`; validate (same step count, fence count + meta, links) else retry once, then skip and report.
  Writes `translated: machine`; panel shows "machine translated — improve on GitHub" link.
  Second mode fills the `.xlf` UI strings with the same masking/glossary.
- Run manually, review the diff, commit — no CI job holding an API key.

**Stage 6 — content build-out** *(ongoing)*: cabinet, sketching, assets (`$import`), components
(`$component` — no example exists yet), docs, calc tutorials; translate.

**Deferred**: docs.archiyou.com (Starlight; needs a standalone `model-viewer` bundle — the old Vite entry
was removed, `vite.config.ts:14` — GLBs precomputed by the Stage 1 headless run, new Caddy vhost).
AI chat: `api.json` + tutorials are its retrieval corpus.

### Cost

| | New code | Runtime deps | Bundle |
| --- | --- | --- | --- |
| Help tool + state + parser + reference | ~920 LOC in 4 files | **none** | ~8 kB gz in the editor chunk |
| Content | lazy raw `.md`, a few kB each, one locale loaded | | on demand |
| `api.json` | generated | | est. 40-60 kB gz, loaded on first Reference/F1 (measure) |
| Tooling | ~330 LOC (2 scripts) | dev only: `typedoc`, `@anthropic-ai/sdk` | 0 |

### Verification

- `pnpm --filter @archiyou/ui test` (parser, highlight ids, per-locale structure equals `en`);
  `pnpm test:core` (every tutorial step and `@example` executes).
- `pnpm dev`, then in the browser (chrome-devtools MCP): clear localStorage → editor opens onboarding,
  spotlight lands on each target incl. after window resize; finish → flag set, no reopen on reload.
- `/editor?tutorial=simple-table` → new script, Next/Back apply code and the model updates; user's
  previous script untouched.
- Type `$comp` + F1 → `$component`; cursor on `.fillet` in `box(1).move(1).fillet(2)` → `Mesh.fillet`;
  on `x.fillet` with unknown `x` → owner list. Follow-cursor toggle updates live.
- Switch to `nl` → translated tutorial; `ar` → RTL prose, LTR code; delete one `nl` file → English fallback.
- `pnpm help:translate --locale nl` twice → second run translates nothing; edit one source step → only that file.
- `pnpm build:editor` → compare bundle size before/after; confirm `api.json` and `.md` are separate lazy chunks.
